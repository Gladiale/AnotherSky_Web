type MaxInfoType = {
  maxCgFolder: number;
  maxStandFolder: number;
  maxVideoFolder: number;
};

const maxInfo: MaxInfoType = {
  maxCgFolder: 3,
  maxStandFolder: 3,
  maxVideoFolder: 1,
};

export { maxInfo, type MaxInfoType };
