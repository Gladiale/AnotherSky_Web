import styles from "./Icon.module.css";
import { GiSnowBottle } from "react-icons/gi";

const ImageSizeControl = () => {
  return <GiSnowBottle className={styles.icon} />;
};

export default ImageSizeControl;
